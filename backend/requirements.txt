# Aura AI 2.0 — Production dependencies
# Auto-compatible with Python 3.12 (Docker) and 3.14 (local)

# Web Framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# Database
sqlalchemy[asyncio]>=2.0.30
asyncpg>=0.29.0
psycopg2-binary>=2.9.9
alembic>=1.13.0

# Cache
redis[hiredis]>=5.0.0

# Auth
email-validator>=2.1.0
pyjwt>=2.8.0
bcrypt==4.0.1

# Config
pydantic>=2.7.0
pydantic-settings>=2.3.0

# HTTP client (AI providers)
httpx>=0.27.0

# Logging
structlog>=24.1.0

# TTS
edge-tts>=6.1.0
nvidia-riva-client>=2.15.0

# Templates
jinja2>=3.1.0

# Utilities
python-dateutil>=2.9.0
cryptography>=41.0.0

# Communication Engine — Voice Pipeline
# webrtcvad requires C++ build tools on Windows; VAD falls back to energy detection
faster-whisper>=1.0.0      # Fast local speech-to-text (OpenAI Whisper backend)
numpy>=1.26.0              # Audio frame processing
scipy>=1.13.0              # Audio resampling (16kHz normalisation)

# Emotion Vision & ML Models
opencv-python-headless>=4.9.0
onnxruntime>=1.18.0

# Dev / Testing
pytest>=8.2.0
pytest-asyncio>=0.23.0
anyio>=4.4.0
aiosqlite>=0.19.0
httpx>=0.27.0
websockets>=12.0           # WebSocket test client
